
<%= form_for @poll,method: :post ,url: poll_vote_path  do |f| %>
 <h3>Poll : <%= @poll.name %><br></h3>

    <% @poll.choices.each do |choice|%>
    <%= radio_button_tag(:radio, choice.id) %>
    <%= label_tag(:tag, choice.name) %><br>

    <% end %>

 <%= f.submit "Vote" %>
<% end %>


